syntax = "proto3";

package small.execution;

// =====================================================================
// small-db protobuf
// =====================================================================
import "src/type/type.proto";

service Insert {
  rpc Insert(Row) returns (WriteResponse) {}
}

service Update {
  rpc Update(RawNode) returns (WriteResponse) {}
}

service Query {
  rpc Query(RawNode) returns (QueryResponse) {}
}

message Row {
  string table_name = 1;
  repeated string column_names = 2;
  repeated string column_values = 3;
}

message RawNode {
  bytes packed_node = 1;
  // For Update RPC: commit_ts (ms). For Query RPC: read_ts (ms).
  // 0 means "use the receiving server's now()" (auto-commit / non-snapshot read).
  int64 ts = 2;
}

message WriteResponse {
  int64 affected_rows = 1;
}

message QueryResponse {
  // Arrow IPC stream bytes for a single RecordBatch (schema + data).
  bytes ipc_bytes = 1;
}
