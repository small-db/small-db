syntax = "proto3";

package small.catalog;

// built-in protobuf types
import "google/protobuf/empty.proto";

// small-db protobuf types
import "src/schema/schema.proto";

service Catalog {
  rpc UpdateTable(small.schema.Table) returns (google.protobuf.Empty) {}
}
