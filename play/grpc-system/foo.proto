syntax = "proto3";

package small.gossip;

service FooService {
  rpc Exchange(Entries) returns (Entries) {}
}

message Entries {
  repeated Entry entries = 1;
}

message Entry {
  string key = 1;
  string value = 2;
}
