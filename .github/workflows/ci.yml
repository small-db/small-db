name: build

on:
  push:
    branches: ["main"]

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Install uv for Python Scripts
        run: wget -qO- https://astral.sh/uv/install.sh | sh
      - name: Install Dependencies
        run: ./scripts/setup/install-deps.sh
      - name: Build
        run: ./scripts/setup/build.sh
      - name: Lint
        # lint go after build as clang-tidy relies on compile_commands.json generated by the build step
        run: ./scripts/format/lint.sh
      - name: Test
        run: ./scripts/test/test.sh
