name: build

on:
  push:
    branches: ["main"]

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        run: wget -qO- https://astral.sh/uv/install.sh | sh
      - name: Check Environment
        run: uv run ./scripts/setup/check-env.py
      - name: Lint
        run: ./scripts/format/lint.sh
      - name: Install Dependencies
        run: ./scripts/setup/install-deps.sh
      - name: Build
        run: ./scripts/setup/build.sh
      - name: Test
        run: ./scripts/test/test.sh